@{
    Layout = "EmptyLayout.cshtml";
}

@model SAM.Taskboard.Model.LoginViewModel

<div class="blur">
    <div class="d-flex justify-content-center h-100">
        <div class="d-flex flex-row justify-content-center align-self-center rounded w-auto bg-light" style="opacity: 0.9">
            <div class="container m-4">
                @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    <div class="row justify-content-center">
                        <label class="mb-3">Добро пожаловать</label>
                    </div>

                    <div class="row justify-content-center">
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "Электронная почта" })
                            @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger small" })
                        </div>
                    </div>

                    <div class="row justify-content-center">
                        <div class="form-group">
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Пароль" })
                            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger small" })
                        </div>
                    </div>

                    <div class="row justify-content-center text-danger small">
                        @Html.ValidationSummary()
                    </div>

                    <div class="row justify-content-center">
                        <button type="submit" class="btn btn-outline-primary ml-2 mr-2">Войти</button>
                    </div>
                }
            </div>

            <div class="d-flex rounded-right p-4 flex-column justify-content-center align-items-center bg-primary text-light">

                <div class="row justify-content-center pb-5">
                    @Html.ActionLink("Зарегистрироваться", "Register", "Account", new { @class = "border-0 btn btn-outline-light" })
                </div>

                <div class="row justify-content-center">
                    @Html.ActionLink("Восстановить пароль", "ResetPassword", "Account", new { @class = "border-0 btn btn-outline-light" })
                </div>

            </div>
        </div>
    </div>
</div>

